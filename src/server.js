const express = require('express');
const morgan = require('morgan');
const data = require('./moviedata.json');
const app = express();
app.use(morgan('dev'));


const API_TOKEN = 

function accessMovie(req, res){

}

app.listen('3000', () => console.log('I hope this works'));